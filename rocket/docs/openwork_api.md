# OpenWork API

Status: deprecated for active Rocket runtime.

Rocket now executes through OpenCode CLI directly. OpenWork notes remain only as historical research in `docs/openwork_reverse_engineering.md`.

Current runtime path:

```text
Nemotron intent -> OpenCode powers verifier -> OpenCode CLI
```

Do not add new Rocket execution features through OpenWork unless the architecture is intentionally reopened.
